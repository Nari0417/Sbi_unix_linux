[root@secrlmsdmgr1 ~]# sudo grep -i "username:" /var/opt/gitlab/gitlab-rails/etc/database.yml
    username: "gitlab"
[root@secrlmsdmgr1 ~]# sudo gitlab-psql -d postgres -c \
> "SELECT datname, pg_get_userbyid(datdba) AS owner
>  FROM pg_database
>  WHERE datname='gitlabhq_production';"
       datname       | owner
---------------------+--------
gitlabhq_production | gitlab
(1 row)
 
[root@secrlmsdmgr1 ~]# sudo gitlab-psql -d gitlabhq_production -c \
> "SELECT extname, pg_get_userbyid(extowner) AS owner
>  FROM pg_extension
>  WHERE extname IN ('pg_trgm','btree_gist');"
  extname   |    owner
------------+-------------
btree_gist | gitlab-psql
pg_trgm    | gitlab-psql
(2 rows)
 
[root@secrlmsdmgr1 ~]# sudo gitlab-psql -d postgres -c "\du"
                                       List of roles
     Role name     |                         Attributes                         | Member of
-------------------+------------------------------------------------------------+-----------
gitlab            |                                                            | {}
gitlab-psql       | Superuser, Create role, Create DB, Replication, Bypass RLS | {}
gitlab_replicator | Replication                                                | {}
